#include <pebble.h>
/*#include "fases.h"

BitmapLayer* createAngelIslandZone(){
    GBitmap *s_bitmap;
    BitmapLayer *s_bitmap_layer;
  
  GRect bounds = GRect(0, 0, 30, 30);
  
  // Load the resource
  s_bitmap = gbitmap_create_with_resource(RESOURCE_ID_BACKGROUND_01);
  
  // Create the BitmapLayer
  s_bitmap_layer = bitmap_layer_create(bounds);
  
  bitmap_layer_set_bitmap(s_bitmap_layer, s_bitmap);
  
  // Set the correct compositing mode
  bitmap_layer_set_compositing_mode(s_bitmap_layer, GCompOpSet);
  
  return s_bitmap_layer;
}*/